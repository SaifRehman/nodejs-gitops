apiVersion: route.openshift.io/v1
kind: Route
metadata:
  labels:
    app: nodejs-api
  name: nodejs-api
spec:
  port:
    targetPort: 3000
  to:
    kind: Service
    name: nodejs-api
    weight: 100
  wildcardPolicy: None